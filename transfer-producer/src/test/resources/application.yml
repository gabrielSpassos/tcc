server:
  port: 8081

spring:
  application:
    name: transfer-producer
  rabbitmq:
    host: 'localhost'
    virtual-host: '/'
    username: 'admin'
    password: 'admin'

rabbitmq:
  transfers:
    exchange: 'transfers-events'
    routing-key: '#'